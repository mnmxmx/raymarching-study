(function(){function r(e,n,t){function o(i,f){if(!n[i]){if(!e[i]){var c="function"==typeof require&&require;if(!f&&c)return c(i,!0);if(u)return u(i,!0);var a=new Error("Cannot find module '"+i+"'");throw a.code="MODULE_NOT_FOUND",a}var p=n[i]={exports:{}};e[i][0].call(p.exports,function(r){var n=e[i][1][r];return o(n||r)},p,p.exports,r,e,n,t)}return n[i].exports}for(var u="function"==typeof require&&require,i=0;i<t.length;i++)o(t[i]);return o}return r})()({1:[function(require,module,exports){
"use strict";

var _WebGL = _interopRequireDefault(require("./modules/WebGL"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

window.onload = function () {
  var webgl = new _WebGL["default"]();
  window.addEventListener('resize', webgl.resize.bind(webgl));
};

},{"./modules/WebGL":3}],2:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _box = _interopRequireDefault(require("./glsl/box.vert"));

var _box2 = _interopRequireDefault(require("./glsl/box.frag"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var Box =
/*#__PURE__*/
function () {
  function Box(props) {
    _classCallCheck(this, Box);

    this.props = props;
    this.init();
  }

  _createClass(Box, [{
    key: "init",
    value: function init() {
      var geometry = this.createGeometry();
      var material = this.createMaterial();
      var box = new THREE.Mesh(geometry, material);
      box.position.copy(this.props.position);
      this.props.scene.add(box);
    }
  }, {
    key: "createGeometry",
    value: function createGeometry() {
      var geometry = new THREE.BoxBufferGeometry(1, 1, 1);
      return geometry;
    }
  }, {
    key: "createMaterial",
    value: function createMaterial() {
      // we use ShaderMaterial in all this series.
      var material = new THREE.ShaderMaterial({
        // prepare vertex shader as string & attach to material
        vertexShader: _box["default"],
        // prepare fragment shader as string & attach to material
        fragmentShader: _box2["default"],
        // prepare uniforms if you need
        uniforms: {
          uColor: {
            value: new THREE.Color(0xffcc44)
          }
        }
      });
      return material;
    }
  }]);

  return Box;
}();

exports["default"] = Box;

},{"./glsl/box.frag":4,"./glsl/box.vert":5}],3:[function(require,module,exports){
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;

var _Box = _interopRequireDefault(require("./Box"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var WebGL =
/*#__PURE__*/
function () {
  function WebGL() {
    _classCallCheck(this, WebGL);

    this.init();
  }

  _createClass(WebGL, [{
    key: "init",
    value: function init() {
      this.setProps();
      this.$container = document.getElementById("wrapper");
      this.renderer = new THREE.WebGLRenderer({
        antialias: true,
        alpha: true
      });
      this.renderer.setClearColor(0xfcfcfc);
      this.renderer.setSize(window.innerWidth, window.innerHeight);
      this.$container.appendChild(this.renderer.domElement);
      this.pixelRatio = window.devicePixelRatio;
      this.renderer.setPixelRatio(this.pixelRatio);
      this.scene = new THREE.Scene();
      var ratio = 0.03; // Perspective camera

      this.camera = new THREE.PerspectiveCamera(this.props.degree, this.props.aspect, this.props.near, this.props.far);
      this.camera.position.set(0, 3, 8); // Orthographic camera
      // this.camera = new THREE.OrthographicCamera(this.props.left * ratio, this.props.right * ratio, this.props.top * ratio, this.props.bottom * ratio, this.props.near, this.props.far);
      // this.camera.position.set(30, 30, 30);

      this.camera.lookAt(this.scene.position);

      for (var i = 0; i < 8; i++) {
        for (var j = 0; j < 10; j++) {
          var obj = new _Box["default"]({
            scene: this.scene,
            position: new THREE.Vector3((j / 10 - 0.5) * 24, 0, -i * 4.0)
          });
        }
      }

      this.render();
    }
  }, {
    key: "setProps",
    value: function setProps() {
      var width = window.innerWidth;
      var height = window.innerHeight;
      var aspect = width / height;
      this.props = {
        degree: 45,
        width: width,
        height: height,
        aspect: aspect,
        fov: 45,
        left: -width / 2,
        right: width / 2,
        top: height / 2,
        bottom: -height / 2,
        near: 0.1,
        far: 10000
      };
    }
  }, {
    key: "render",
    value: function render() {
      // const delta = this.time.getDelta();
      this.renderer.render(this.scene, this.camera);
      requestAnimationFrame(this.render.bind(this));
    }
  }, {
    key: "resize",
    value: function resize() {
      this.setProps();
      this.renderer.setSize(this.props.width, this.props.height);
      this.camera.aspect = this.props.aspect;
      this.camera.updateProjectionMatrix();
      this.camera.updateProjectionMatrix();
    }
  }]);

  return WebGL;
}();

exports["default"] = WebGL;

},{"./Box":2}],4:[function(require,module,exports){
module.exports = "varying vec3 vPos1;\r\nvarying vec3 vPos2;\r\nuniform vec3 uColor;\r\nvoid main(){\r\n  vec3 color = uColor - vPos2.z * 0.01;\r\n  color -= vPos1 * 0.1;\r\n  gl_FragColor = vec4(color, 1.0);\r\n}";

},{}],5:[function(require,module,exports){
module.exports = "varying vec3 vPos1;\r\nvarying vec3 vPos2;\r\nvoid main(){\r\n  vec4 worldPosition = modelMatrix * vec4(position, 1.0);\r\n  vPos1 = position;\r\n  vPos2 = worldPosition.xyz;\r\n  gl_Position = projectionMatrix * viewMatrix * worldPosition;   \r\n}";

},{}]},{},[1]);
